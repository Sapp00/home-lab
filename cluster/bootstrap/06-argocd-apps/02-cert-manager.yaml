---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "cert-manager"
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/sapp00/home-lab.git"
    path: cluster/bootstrap/02-cert-manager
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true
